#!/bin/bash
# SessionStart hook: prompt Claude to sync Google Calendar → vault
echo "gcal-sync: Google Calendar MCP is available. Sync upcoming events into today's daily note and tasks.md. Ask the user first."
